spring:
  application:
    name: ms-onboarding-data-store-api

aws:
  region: us-east-1

  endpoint-url: http://localhost:4566

  access-key: test

  secret-key: test

  sqs:
    queue-url: http://localhost:4566/000000000000/onboarding-vault-capture.fifo

  s3:
    bucket: onboarding-vault-hnd-local

  dynamodb:
    table: onboarding-vault-idempotency

  eventbridge:
    bus-name: onboarding-bus
